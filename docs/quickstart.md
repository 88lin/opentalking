# Quickstart

1. Create a virtual environment and install `opentalking`.
2. Copy `.env.example` to `.env`.
3. Download required models.
4. Start either `opentalking-unified` or the distributed API/worker/server stack.
